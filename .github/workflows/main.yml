name: CiCd Pipeline in GitHub Actions

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: action/checkout@v5
      - name: My First Step

      run: | 
        echo "Hello world from the CiCd Pipeline in GitHub Actions"
        uname -a
